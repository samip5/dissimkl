[package]
name = "dissimkl"
# Never a release number: the git tag is the authoritative version, stamped in
# by build.rs. This only seeds the string dev builds report, so it cannot drift
# out of sync with what has actually been released.
version = "0.0.0-dev"
edition = "2021"
description = "Discord Rich Presence for Simkl"
authors = []
license = "MIT"

[[bin]]
name = "dissimkl"
path = "src/main.rs"

[dependencies]
discord-rich-presence = "1.1"
ureq = { version = "3", features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
configparser = "3"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "env-filter"] }
lru = "0.18"
open = "5"
winit = "0.30"
anyhow = "1"
dirs = "6"
dark-light = "2"
scraper = "0.27"

[target.'cfg(target_os = "linux")'.dependencies]
ksni = { version = "0.3", features = ["blocking"] }

[target.'cfg(not(target_os = "linux"))'.dependencies]
tray-icon = "0.24"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
